{
  "name": "futurefit-eg",
  "private": true,
  "description": "FutureFit storefront monorepo — Railway deploys the API from /server",
  "scripts": {
    "build": "npm install --prefix server && npm run db:generate --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18"
  }
}
